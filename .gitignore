# Compiled class files
*.class

# Build output
target/
build/
out/
bin/

# Maven
# NOTE: .mvn/wrapper/maven-wrapper.properties IS committed on purpose — it pins the Maven
# version so `./mvnw` gives everyone the same build. Only the downloaded jar is ignored.
.mvn/wrapper/maven-wrapper.jar
HELP.md

# Gradle (in case we switch)
.gradle/

# Logs
*.log

# IDE
.idea/
*.iml
*.iws
*.ipr
.vscode/
.settings/
.project
.classpath

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Environment / secrets (never commit these)
.env
*.env
application-*.properties
!application-*.properties.example
application-secret.*

# Docker local volumes
pgdata/


# Claude (local-only AI working files, never pushed)
CLAUDE.md
INDEX.md

# Local Docs
/docs/local/*

# Spring Initializr leftovers (project already extracted into the repo root)
starter.zip
_spring_tmp/

# Java upgrade tooling scaffolding (IDE-generated, not ours)
.github/modernize/
# Frontend (pms-ui)
node_modules/
dist/
dist-ssr/
build/
.vite/
*.local
*.tsbuildinfo
.eslintcache
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.env.local
.env.*.local
